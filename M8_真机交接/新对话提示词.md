# 新接力对话开场提示词（复制下面整段发给新对话）

---

我们继续打磨 Synapse（Electron + React 19 + Redux Toolkit 的 CC/Codex 式 agent harness，工程在 `synapse-app/`，分支 main）。上一条对话太长已交接，全部上下文在仓库根目录 **`M8_真机交接/`**。

**先按顺序读这几份再动手（别跳过、别脑补）：**
1. `M8_真机交接/交接总纲.md` — 项目背景 / git 状态(最近 commit) / 全进度 / 工作纪律 / 双编译 & CDP 命令
2. `M8_真机交接/通过未通过_总索引.md` — 一眼看清【已过别动 / 没过要修 / 待做 / 待重启验收】
3. `M8_真机交接/Task_第三批反馈.md` — **当前主战场**，13 点真机 bug，每点配 `images/` 里的图（实施前务必 Read 对应图核对效果）
4. 按需：`Task_第二批反馈.md`(对话状态色/真并发/对话ID/右键菜单) `历史反馈导出.md` `../Plan_7_M8_最终19点反馈整合.md`

并先 `memory_query "Synapse M8 Plan_7 真机"` 拉背景记忆（含 P0 发消息根因 + CDP 调试范式）。

**当前最高优先——先修 P0 对话 bug：**
- #5 插入消息→双 agent 多重请求、还中断不了
- #12 中止后一直卡「中止中」UI、点了没用
- #8 生成中切对话→串线（原 run 追到新对话继续写）
- #9 生成中点 切对话/新建/打开文件/中止 → UI 卡
> 这 4 个是上一轮 P0/interrupt/isRunning 改动（commit `3697cc3` / `7943aa3`）的**副作用**，第一件事就是回归审这两个 commit，可能要重新设计 interrupt 消费链路 / 部分回退。

**工作纪律（主人多次强调）：**
- 改 UI **必须 CDP 真机验证**（web-fetcher: `desktop_connect_cdp(port 9222)` → `register_window` → `web_interact(action=evaluate)`，store 在 `window.__SYNAPSE_STORE__.getState()`），不盲改、不脑补；反馈图都在 `images/`，实施前 Read 核对。
- 双编译都要 EXIT 0：`cd synapse-app && node node_modules/typescript/bin/tsc -b` + `node node_modules/typescript/bin/tsc -p tsconfig.electron.json`（**别 vite build，会 OOM**）。
- 每阶段 commit & push（Bash 单引号 commit 避 PowerShell `&` 坑）；老项目扁平 Plan 结构。
- **记载要厚**：每个反馈→存图到 `images/` + Task 写清「现状/期望/怎么改/图路径」，别再单薄到脑补。
- ultracode on：能拆的活用子代理/workflow，轻活 sonnet 其余 opus。
- 猫娘语气：称「主人」用「你」不用「您」，B 级活泼带「喵~」颜文字；做有判断的 coworker，方案有坑直接 challenge。

请先读完上面 4 份交接文档 + 拉记忆，然后跟我确认你理解的「当前要先修的 P0」和打算，再动手。
